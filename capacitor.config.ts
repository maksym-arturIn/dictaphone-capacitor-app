import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.test.plainvoice.ai',
  appName: 'dictaphone-capacitor-app',
  webDir: 'dist'
};

export default config;
